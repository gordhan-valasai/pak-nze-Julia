# v0.4.0 Smoke Test (Phase 4 Pause)

Date: 2026-04-30  
Command run: `julia --project=. src/solve.jl --scenario REF --solver highs --output-tag ref_v040_smoke`

## Scope note

The requested "REF scenario only, single year" smoke test is not directly executable in the current model because multiple constraints and reporting paths index explicit year `2050`.  
To avoid feature changes outside the unit refactor, this smoke test uses REF full-horizon solve and checks dimensional consistency in the resulting outputs.

## Solver result

- Status: **OPTIMAL**
- Objective (solver): `6.0608688126e+11` USD
- HiGHS iterations: `6419`

## Dimensional consistency checks

1. **Capacity reporting conversion (MW -> GW)**
   - `ref_v040_smoke_capacity_summary.csv` reports:
     - `total_installed_capacity_2024_gw = 70.5155`
     - `total_installed_capacity_2050_gw = 172.5563`
   - Values are in plausible GW magnitude and consistent with MW-to-GW conversion at output.

2. **Emissions reporting conversion (tonnes -> Mt)**
   - `ref_v040_smoke_sector_emissions.csv` values are reported in Mt-equivalent scale:
     - 2024 power emissions: `34.5762`
     - 2050 power emissions: `20.0461`
   - Output magnitude confirms tonne-to-Mt conversion is being applied once at reporting.

3. **Objective reporting basis (USD)**
   - `ref_v040_smoke_summary.csv` writes `objective_usd = 6.060868812610028e11`.
   - This is aligned with canonical objective unit (USD), with no forced billion-scaling in solver objective itself.

4. **Generation reporting conversion (MWh -> TWh)**
   - Summary and provincial tables are produced successfully with TWh columns, indicating annual/slice energy variables are resolving under MWh convention.

## Smoke-test conclusion

- **PASS (Phase 4 smoke):** model builds and solves after unit refactor through variables/parameters/constraints/objective.
- No compile-time symbol breakages detected in solve path.
- Proceeding to Phase 5 should now focus on solution container and post-processing cleanup under the same canonical unit contract.
