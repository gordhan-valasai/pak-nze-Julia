# v0.4.0 Unit Refactor Log (Phases 2-4)

## Phase 2
- Added canonical unit reference: `results/v0.4.0_unit_system.md`.

## Phase 3
- Renamed core capacity variables in `src/variables.jl`:
  - `new_capacity_gw` -> `new_capacity_mw`
  - `installed_capacity_gw` -> `installed_capacity_mw`
  - `retired_capacity_gw` -> `retired_capacity_mw`
- Renamed core emissions variables in `src/variables.jl`:
  - `emissions_mtco2` -> `emissions_tco2eq`
  - `gas_emissions_mt` -> `gas_emissions_tonnes`
  - `negative_emissions_mtco2` -> `negative_emissions_tco2eq`
- Renamed/standardized parameter fields in `src/parameters.jl`:
  - `capex_usd_per_kw` -> `capex_usd_per_mw`
  - `fixed_om_usd_per_kw_yr` -> `fixed_om_usd_per_mw_yr`
  - `max_potential_gw` -> `max_potential_mw`
  - `max_buildrate_gw_per_year` -> `max_buildrate_mw_per_year`
  - `carbon_price_usd_per_tco2` -> `carbon_price_usd_per_tonne_co2`
  - `gas_emission_factor_mt_per_mwh` -> `gas_emission_factor_tonnes_per_mwh`
  - `negative_emission_potential_mtco2` -> `negative_emission_potential_tco2eq`
  - `negative_emission_cost_usd_per_tco2` -> `negative_emission_cost_usd_per_tonne_co2`
- Applied load-time conversions in `src/parameters.jl`:
  - CAPEX and fixed O&M from source USD/kW and USD/kW-yr to canonical USD/MW and USD/MW-yr (`*1000` on load).
  - Existing capacity from source GW to canonical MW (`*1000` on load).
  - Build limits from source GW to canonical MW (`*1000` on load).
  - Negative-emissions potential from source Mt to canonical tonnes (`*1e6` on load).
  - Scenario targets `electrolyser_capacity_gw_2040` and `battery_storage_target_gw_2050` to MW (`*1000` on load).

## Phase 4
- Updated capacity constraints in `src/constraints_capacity.jl` to use MW variable/parameter families consistently.
- Updated emissions constraints in `src/constraints_emissions.jl`:
  - Removed legacy `*1000` activity patch; emissions now directly use `annual_activity_mwh * tonnes_per_mwh`.
  - Negative-emissions ceiling uses canonical tonnes parameter directly.
- Updated objective in `src/objective.jl`:
  - Removed non-canonical `*1000` factors in CAPEX/FOM terms.
  - Removed `*1e-6` factors in carbon and negative-emission cost terms (variables are canonical tonnes).
- Updated module constraints in `src/modules/power.jl` and `src/modules/hydrogen.jl` to align with MW-based capacity families.
- Updated `src/solve.jl` and `src/solution_container.jl` references to renamed symbols to keep build path consistent through Phase 4.

## Smoke test note
- Original prompt requested REF single-year smoke test, but model contains explicit hard-coded references to year `2050` in multiple constraints and summary extracts.
- For Phase-4 pause validation, smoke test is executed as REF full-horizon solve with dimensional consistency checks, documented in `results/v0.4.0_smoke_test.md`.
